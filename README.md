# EscanerDePuertos

Se crea fichero de Python al cual le indicas la IP a la que quieres escanear los puertos abiertos y te muestra por pantalla los puertos abiertos, se realiza conexión por TCP con lo cuál puede no indicar que esten abiertos si solo permiten conexión UDP
